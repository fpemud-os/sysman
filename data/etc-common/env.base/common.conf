INSTALL_MASK="${INSTALL_MASK} /etc/init.d /etc/conf.d /etc/rc.d"          # removing sys-apps/openrc init files
INSTALL_MASK="${INSTALL_MASK} /lib/netifrc"                               # removing net-misc/netifrc scripts
INSTALL_MASK="${INSTALL_MASK} /etc/default /etc/skel"                     # they are not used since we use pkg-fpemud/fpemud-refsystem
INSTALL_MASK="${INSTALL_MASK} /etc/prelink.conf.d"                        # for simplicity, don't use prelink
INSTALL_MASK="${INSTALL_MASK} /etc/cron.*"
INSTALL_MASK="${INSTALL_MASK} /etc/logrotate.d"
INSTALL_MASK="${INSTALL_MASK} /etc/xinetd.d"
INSTALL_MASK="${INSTALL_MASK} /etc/grub.d"
INSTALL_MASK="${INSTALL_MASK} /etc/sudoers.d"
INSTALL_MASK="${INSTALL_MASK} /etc/kernel"
